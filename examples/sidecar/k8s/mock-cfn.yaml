apiVersion: v1
kind: Service
metadata:
  name: mock-cfn
  namespace: a2a-demo
spec:
  selector:
    app: mock-cfn
  ports:
  - port: 9002
    targetPort: 9002
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mock-cfn
  namespace: a2a-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mock-cfn
  template:
    metadata:
      labels:
        app: mock-cfn
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      containers:
      - name: mock-cfn
        image: mock-cfn:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 9002
